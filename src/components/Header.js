import React from "react";
import styles from "./Header.module.css";
const Header = () => {
  return (
    <nav className={styles.nav}>
      <header>Frontend Task A</header>
    </nav>
  );
};

export default Header;
